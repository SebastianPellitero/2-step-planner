export default function ExplorePage() {
  return (
    <div style={{ height: '100%', position: 'relative' }}>
      <p style={{ padding: '2rem', color: 'var(--color-muted)' }}>
        Explore page coming soon — Mapbox map + live location + nearby places.
      </p>
    </div>
  )
}
